apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: analytics-service
  labels:
    cloud.googleapis.com/location: asia-south1
  annotations:
    run.googleapis.com/ingress: all
    run.googleapis.com/ingress-status: all
spec:
  template:
    metadata:
      annotations:
        autoscaling.knative.dev/minScale: '1'
        autoscaling.knative.dev/maxScale: '20'
        run.googleapis.com/cloudsql-instances: PROJECT_ID:asia-south1:trading-platform-db-replica-prod
        run.googleapis.com/vpc-access-connector: trading-platform-connector
        run.googleapis.com/vpc-access-egress: private-ranges-only
        run.googleapis.com/cpu-throttling: 'false'
        run.googleapis.com/startup-cpu-boost: 'true'
    spec:
      containerConcurrency: 40
      timeoutSeconds: 600
      serviceAccountName: trading-platform-sa@PROJECT_ID.iam.gserviceaccount.com
      containers:
      - name: analytics-service
        image: gcr.io/PROJECT_ID/analytics-service:latest
        ports:
        - name: http1
          containerPort: 8082
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: database-replica-url
              key: latest
        - name: REDIS_HOST
          valueFrom:
            secretKeyRef:
              name: redis-host
              key: latest
        - name: REDIS_PORT
          value: '6379'
        - name: INFLUXDB_URL
          valueFrom:
            secretKeyRef:
              name: influxdb-url
              key: latest
        - name: INFLUXDB_TOKEN
          valueFrom:
            secretKeyRef:
              name: influxdb-token
              key: latest
        - name: INFLUXDB_ORG
          value: 'trading-platform'
        - name: INFLUXDB_BUCKET
          value: 'market-data'
        - name: JWT_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: jwt-secret-key
              key: latest
        - name: ENVIRONMENT
          value: 'production'
        resources:
          limits:
            cpu: '1000m'
            memory: '1Gi'
          requests:
            cpu: '250m'
            memory: '256Mi'
        livenessProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /health
            port: 8082
          initialDelaySeconds: 0
          periodSeconds: 5
          timeoutSeconds: 5
          failureThreshold: 12
  traffic:
  - percent: 100
    latestRevision: true
